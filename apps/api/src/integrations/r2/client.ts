/**
 * Cloudflare R2 (S3-compatible) storage client placeholder.
 * Use AWS SDK v3 with R2 endpoint for signed uploads/downloads.
 */
export const r2Client = {
  // Placeholder — S3Client configured for R2
} as const;
