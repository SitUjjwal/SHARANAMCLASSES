/**
 * TestTimer — thin wrapper around reusable Timer for test attempts.
 * Keeps pause-in-background, low-time warning, and auto-submit wired.
 */
export { Timer as TestTimer } from '@/components/ui/timer';
export type { TimerProps as TestTimerProps } from '@/components/ui/timer';
